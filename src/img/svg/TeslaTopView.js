import React from "react";
import Svg, { G, LinearGradient, Stop, Image, Path, Polygon, Rect, Ellipse } from "react-native-svg";
import { View } from 'react-native';

const SvgComponent = props => (
  <View>

  </View>
);

export default SvgComponent;